/**
 * The finale film: a silent 720x1280 visual bed cut to the exact length of
 * keeper-lock4.mp3. The narration clock is the master; the film never
 * carries timing. Both files are produced by scripts/prepare-cinema.mjs
 * and committed. A missing film degrades to poster-only at runtime.
 */
export const CINEMA_FILM_PATH = "/cinema/finale.mp4";
export const CINEMA_POSTER_PATH = "/cinema/poster.webp";
